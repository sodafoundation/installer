BASE=${BASE:-/srv}
BASE_ETC=/etc
DIR=
STATES=salt
PROFILES=${PROFILES:-pillar}

if [[ `uname` == 'FreeBSD' ]]
then
    BASE=/usr/local/etc
    BASE_ETC=/usr/local/etc
    DIR=salt
    STATES=states
fi
